<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task.completed ? "✅---" : "⬜---" %><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<p><%= link_to 'Add a task', new_path %></p>
